<UserControl x:Class="AquaAssist.View.MonitorizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AquaAssist.View"
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="500">
    <Grid>
        <ScrollViewer>
            <StackPanel Orientation="Vertical"> 
                <local:SensorView DataContext="{Binding TemperatureSensor}" Height="500"></local:SensorView>
                <local:SensorView DataContext="{Binding FlowRateSensor}"  Height="500"></local:SensorView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
